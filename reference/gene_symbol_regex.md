# Regular expression for gene symbols

A regex pattern that matches prokaryotic gene symbols of the form
`abcA1` (three lowercase letters, uppercase letter(s), optional digits).

## Usage

``` r
gene_symbol_regex
```

## Format

A length-1 character vector.
