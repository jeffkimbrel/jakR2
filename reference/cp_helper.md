# Internal helper function for parsing card.json file

This helper function is called in parallel by the
[`card_parse()`](https://jeffkimbrel.github.io/jakR2/reference/card_parse.md)
function, and isn't intended to be accessed directly.

## Usage

``` r
cp_helper(card)
```

## Arguments

- card:

  an inner card.json object
